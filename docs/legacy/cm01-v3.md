# CM-01 V3 — archived golden dossier

Audience: both · Diátaxis: explanation · Kind: dated-study

> **Status: legacy / static / non-executable.** CM-01 has been removed from active
> source, configuration, scripts, catalogues and UI fixtures. This page is the only
> documentation entry point for the retained evidence.

Two evidence locations remain:

- the static golden fixture and its checksum under
  [`state/fixtures/retired/cm01-v3/`](../../state/fixtures/retired/cm01-v3); and
- the original immutable project, thread, capture and attempt records under
  `state/local/` (gitignored).

The fixture records a bounded historical comparison across SysON, CAD, Modelica, ERPNext
and an isolated CalculiX handoff. It is an audit input only. The `state/local/` records
preserve the original revisions and hashes; they must not be moved, rewritten, replayed
or attached to another project.

There is no CM-01 project template, registry entry, operation, executor, runner, gate,
catalogue, preview fallback or provider admission. Reading either retained location does
not authorize a new provider call and does not establish current evidence, manufacturing
readiness or certification.

## Active replacement boundary

The active product route is generic and focus-only. The Workbench reads the durable
focus or an explicit project ID; it does not seed or fall back to CM-01.
`desk-lamp-dl04` is the generic vehicle, not a committed `@2` golden. A
`verify.run-fea-static-proof@2` success is only a captured, reread Thread revision under
`state/local/` (gitignored). Absence is `unavailable`. Do not relabel `@1`.

The mechanical proof seal and the CalculiX `@2` execution use different human
authorities. The earlier seal links the reviewed declaration to
`verify.seal-proof-case@1`; the later run needs its own MRTR and
`resolved-operation-plan/2.0`. See the
[mechanical proof-case reference](../reference/contracts/mechanical-proof-case.md) for the exact
separation.
