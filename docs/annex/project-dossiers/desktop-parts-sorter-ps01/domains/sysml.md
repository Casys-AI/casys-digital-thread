# PS-01 SysML

Audience: both · Diátaxis: reference · Kind: project domain status

Thread r4 seals one `DesktopPartsSorter` `PartDefinition`, six component definitions,
six exact typed occurrences and nine technical `AttributeUsage` handles.

The live provider omitted typed usages when the full textual package was inserted. The
renderer recovery created native `PartUsage` plus `FeatureTyping`, set the target through
code-owned AQL and reread the exact target identity before publication. PS-01 r3 is the
positive runtime proof for that recovery; r4 enriched the same inherited identities.

The nine handles are `frame_length`, `frame_width`, `frame_height`, `arm_length`,
`arm_width`, `arm_height`, `targetCommand`, `responseRate` and `loadResistance`. They
provided the compiler's exact `parameterizes` joins for the three admitted single-root
verticals. Ports, flows, placements and behaviors remain outside this capture.
