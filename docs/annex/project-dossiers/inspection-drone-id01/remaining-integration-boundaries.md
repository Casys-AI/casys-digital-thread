# ID01 - remaining integration boundaries

Observed 2026-09-06 UTC / 2026-09-07 Asia/Taipei. This is a proposal and gap record, not
implemented coverage, a new authority, or approval to weaken existing contracts.

## Root assembly capability

This boundary was crossed after explicit operator authorization. InspectionDrone now has
a canonical root from six exact canonical child modules, sealed at Thread r66, followed
by real L3/L4/L5 at r67-r69. All five geometric criteria passed, with zero gate claims.
The [exact canary ledger](nested-root-canary-20260907.md) separates fixed-runtime proof
from the independently reviewed source tests for two-hop retirement. Bounded manual
two-level coverage is promoted; schemas, runtime pins and per-invocation limits are
unchanged. No automatic ancestor rebuild, deeper/unbounded nesting, mixed-input runtime
case or physical-joint conclusion is promoted.

## Physical integration geometry

The initial unexecuted camera layout was rejected. The captured and executed root
successor puts the camera bracket base beneath the deck, with its footprint inside the
plate. The counterpart deck holes were still absent from that canary.

A [camera/deck counterpart proposal](sources/camera-deck-interface-provenance.md) now
has an original source successor `id01-central-deck@2`, SHA-256
`8daabdc7553683dcd28109a53157f0e0012847063652f0258109cebda576d924`. Its three new bare
parameter handles were added through reviewed `model.write-architecture@1` at Thread
r70, architecture
`architecture-8fdff1a0ac8c93b98e4c70074a32788044b724955d067bcaf4e384bb984e99f1`. The
file was captured at workspace r108 and attachment `id01-central-deck-attachment@4`
recrossed at r109. Source analysis passed, with eight named numeric levers, capture
SHA-256 `70a853aa9adc12b0c60dab98561e7eaaf221c94b8774a6d2b0203ecd623f88d1`. Compilation
preview was refused before admission; no new deck STEP or affected module rebuild exists
yet.

Motor-to-arm, avionics-carrier-to-frame, power-tray-to-deck, landing-to-deck and camera
attachments remain to be represented and reviewed explicitly. Nominal arm/deck hole
alignment is not bolt design or strength. Purchased-component models are envelope
references, the battery is a reserved volume, and the propeller is a static original
proxy. These labels remain part of the design truth.

## Verification boundaries

The current admission blocker is requirements recross, not source parsing or missing
geometry. The r50 CameraMountBracket requirements capture still consumes architecture
r7. The exact compilation-basis resolver requires active requirements to bind the active
architecture and therefore refuses r70. A read-only invocation of the real resolver on
persisted state resolved r69 (107 elements) and refused r70 at
`assertRequirementsInputs`: `requirements artifact ... inputs are not exact`.

The existing `model.write-requirements@1` is not an unchanged-requirements recross
route: identical metric/operator/value/unit is adopted, and an empty insertion plan is
refused before a provider write. Archiving the valid requirement, adding a fake metric,
or relaxing architecture equality is not an admissible workaround.

Proposed next extension, not implemented or registered: an explicit requirement recross
authority with a fresh human-reviewed current basis, exact predecessor/target/
constraint readback, a versioned successor capture, preserved historical evidence and no
implicit reuse of old evaluation authority. It needs its own bounded runbook and
operation/capture contract. The current generic SysML extension route covers renderer
mutations or agent-authored source constructs, not this provider-read recapture. This
authority decision remains open.

- Six immediate modules have real L3 observations and independent L4 passes. After F07
  was corrected and locally adopted, all six received fresh provider-free L4 recrosses
  and [L5 accepts with zero gate claims](module-closeouts-20260907.md). They are not an
  satisfaction of a whole-drone gate. The separate root now has its own bounded checks.
- F07's first code pass was rejected for historical-replay regression. The follow-up
  passed 90 targeted tests and type checking, was independently reviewed, integrated,
  and exercised through the six real local closeouts. No provider image was changed.
- The camera bracket has a separate requirement-linked, executed and accepted
  [theoretical bench proof](camera-bracket-bench-result-20260907.md). It does not answer
  the root mounting or flight question.
- The assembly L5 public selector requires a current-tip L4 result. Its `unavailable`
  state after the FEA branch was resolved by exact L3-dependent L4 recrosses, each
  immediately followed by its reviewed L5. No old proposal or L3 fact was altered.

No make/buy, flight, fabrication, procurement or certification branch has been opened.
