# TPS03 — CAD and canonical assembly

Audience: both · Diátaxis: explanation · Kind: dated execution trace

Each child geometry was authored as two workspace files: a scalar-dimensions leaf and
a Build123d root joined by one direct edge. The server captured, reviewed and lowered
each exact closure; the agent did not select a provider, tool or runtime.

| Child | Individual admission | Canonical Thread | Outputs |
| --- | --- | --- | --- |
| StandBase | `technical-compilation-admission-bafdffe0d20cb0b6cbbd9ad2830f4a34d6451165f3d7b97a6104cb20eb331bd6` | r7 | STEP 15,431 B; GLB 3,292 B |
| StandBackrest | `technical-compilation-admission-29bc34efc07716dc47b26572c1ac0aeb86015ff7796c4b58c46d680559069b68` | r9 | STEP 15,435 B; GLB 3,292 B |

The first combined r5 admission was valid analysis evidence but contained two ready
roots, while the canonical child-export contract requires one. Individual exact
admissions were therefore sealed at r6 and r8. This is an AX preflight opportunity,
not evidence that multi-root canonical export already exists.

At r10 the exact product structure and two immediate placements were captured. The
canonical r11 module reopened both child STEP captures and the placement source, then
sealed one assembly STEP of 32,726 bytes and one GLB of 6,336 bytes. Exact digests are
in [runtime evidence](../runtime-evidence.md).

The result is a real static geometric assembly of two solids. It establishes no joint,
clearance envelope, permitted motion, load behavior, manufacturability or product
fitness. Those require separate registered oracles.
