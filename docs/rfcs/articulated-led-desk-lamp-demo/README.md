Audience: agent · Diátaxis: none · Kind: RFC Status: active

# RFC: articulated LED desk lamp — fast-agent implementation queue

This is an execution brief for a fast coding agent, not a product contract, a delivery
calendar, or proof that a named capability already exists. Once a lot is merged, living
truth belongs in the relevant how-to and reference pages. The current coverage pages
remain authoritative until an implemented, qualified path and its evidence change them.

The goal is one deliberately bounded, inspectable articulated LED desk-lamp demo: a
human can see the product story and inspect distinct mechanics, thermal, circuit, impact
and decision evidence for one exact project revision. It is not a certification claim, a
manufactured-product release, generic multiphysics, or a promise of modal analysis.

Before code, read the [human input gates](00-human-input-gates.md). They separate work
Grok may consume automatically from physical and consequential decisions that remain
human-owned. Missing inputs do not justify defaults; they park only the dependent live
evidence lot, not unrelated generic implementation.

## Non-negotiable boundaries

- An engine returns technical facts. CalculiX, OpenModelica and ngspice are engines;
  none is an oracle or an L5 decision. SysON constraint evaluation owns reviewed
  requirement comparison; an explicit human record owns the consequential decision.
- The agent proposes and executes registered operations only. It does not choose a
  native tool, command, solver deck/netlist envelope, runtime/image alias, path, mesh
  setting, provider credential, raw SysML, or raw SysON request.
- Mechanics consumes exact canonical part STEP where its method requires geometry; a
  `cad-model`, preview, parser success or private isolated output is not a substitute.
- New lamp data may flow through an existing qualified surface. A new analysis family,
  metric, unit, source grammar, lowerer, worker output, recovery rule or evaluation
  projection is shared capability work: closed contract through WAL/replay, never an
  extra catalog field or a lamp-specific branch.
- `pass` is an L4 evaluation, not L5. `unavailable`, `unresolved`, `error`,
  `provisional`, `documentary`, `unverified`, `demo`, `TRACE GAP` and `UNLINKED` stay
  literal. No screen, solver exit or successful queue action fills a missing link.
- This queue makes no modal promise. Do not add modal schemas, operations, catalog rows,
  tests or documentation under the banner of the lamp demo.

## Directly copyable start prompt for Grok

```text
You are implementing the Casys articulated LED desk-lamp demo RFC queue.

Read AGENTS.md, docs/rfcs/README.md, this README, the first non-complete sibling lot,
and every linked living contract before editing. Inspect the current code and git status
before acting; docs/RFC wording is not evidence that an operation exists. Treat the
first non-complete lot as your whole scope. Preserve unrelated dirty worktree changes.

Use server-owned registered operations and closed contracts only. Never invent native
solver/netlist/SysML/provider payloads, choose runtime aliases, turn an engine success
into an oracle verdict, or claim a human L5 decision. Keep literal gaps literal.

Implement the smallest causal change, run the lot's focused checks, audit exact staged
paths and whitespace, then make one atomic commit. Update the Progress journal in this
README with the lot, commit, validation and next permitted lot. Continue automatically
to the next dependency only while all checks are green and no human gate is reached.
Stop only at a named human gate or a concrete blocking fact; report exact evidence,
preserve durable state, and do not compensate with a special case or broad rewrite.

Do not run global repository checks after every lot. Run them only in 10 integration
closeout. Do not push, publish, reset, rebase, force-push, stage unrelated files, or
modify docs/rfcs/README.md unless separately authorized.
```

## Ordered queue

Sizes express implementation scope only: `S` small, `M` medium, `E` extension-sized.
They are not time estimates. Read and execute the sibling files in queue order; a file
is not permission to work ahead of its dependencies.

| Queue | File                                                                         | Size | Lane        | Depends on                                 | Completion boundary                                                                                               |
| ----- | ---------------------------------------------------------------------------- | ---- | ----------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| 01    | [demo contract](01-demo-product-contract.md)                                 | S    | core        | current repository                         | One product story, exact demo limits, success/evidence matrix, and no invented authority.                         |
| 02    | [SysML architecture and parameters](02-sysml-architecture-and-parameters.md) | M    | core        | 01                                         | Declared lamp structure/handles and bounded requirement inputs through the registered renderer path.              |
| 03    | [mechanical proof](03-mechanical-arm-correction-loop.md)                     | M    | core        | 01, 02                                     | One narrow static mechanical question, real fail-only correction and successor proof on exact canonical geometry. |
| 04    | [Modelica thermal](04-modelica-thermal-vertical.md)                          | M    | core        | 01, 02                                     | One admitted thermal observation chain plus its own qualified L4 and human L5 boundary.                           |
| 05    | [ngspice LED circuit](05-ngspice-led-driver/README.md)                       | E    | core        | 01, 02                                     | One separate closed electrical method, replayable evidence and qualified evaluation.                              |
| 06    | [cross-domain impact](06-cross-domain-impact-loop.md)                        | M    | core        | 03–05                                      | One reviewed power/brightness change invalidates only exact dependent branches; no hidden cascade.                |
| 07    | [Workbench demo projection](07-workbench-demo-story.md)                      | S    | core        | 01–06                                      | Read-only projection of persisted before/after, branch and impact facts; no command or provider authority.        |
| 08    | [DDD refactor](08-ddd-refactor-queue.md)                                     | M    | refactor    | findings from 01–07                        | Causal boundary cleanup only; no opportunistic architecture rewrite.                                              |
| 09    | [algorithmic hardening](09-algorithmic-hardening-queue.md)                   | M    | refactor    | 08 and demonstrated failure modes          | Deterministic/correctness hardening traced to measured risks.                                                     |
| 10    | [integration and demo closeout](10-integration-demo-closeout.md)             | S    | integration | accepted core; accepted refactor work only | Persisted-proof, replay, living-doc, demo-script and exact-scope closeout.                                        |
| 11    | [stable engineering lifecycle](11-stable-engineering-lifecycle.md)           | E    | refactor    | observed UI lifecycle defects               | Explicit activity/revision/attempt identity from Project through the read-only Workbench.                         |

Core demo lane is `01 → 02 → {03, 04, 05} → 06 → 07 → 10`. Lots 03, 04 and the
contract-only beginning of 05 may advance independently after 02 when their paths do not
overlap. Electrical and impact are part of the agreed product story: parking either
keeps the **demo** blocked, even when the current repository remains healthy. Lots 08
and 09 are refactor lanes; execute their atomic items whenever a vertical exposes the
named debt, but do not make speculative cleanup a prerequisite for unrelated evidence.

## Automatic continuation and stop rules

After a green lot: commit only that lot, update the Progress journal, and start its next
allowed dependency automatically. Do not wait for permission between green, bounded
lots. A defect discovered later belongs to the earliest owning lot; make a small causal
follow-up there rather than hiding compatibility logic downstream.

Stop and request a human decision—not an invented default—when:

- the lamp's intended use, physical assumptions, criterion threshold, material, source
  value, geometry basis, circuit scenario, impact condition or accepted consequence is
  not already explicitly reviewed;
- an MRTR or L5 decision is required;
- a new metric needs a SysON write → extract → evaluate unit proof, or criteria do not
  have defined comparison semantics;
- the method would require raw solver/netlist/provider arguments, an arbitrary source
  escape hatch, unpinned runtime/image, a noncanonical geometry substitute, or a
  lamp-specific execution branch; or
- engine/isolation/oracle recovery is ambiguous. Preserve state and report `unresolved`
  or `error`; never redispatch a non-idempotent call to manufacture a cleaner outcome.

An ordinary local coding failure is not a human gate: diagnose it against the current
lot, apply the smallest causal repair, and rerun focused validation. An unrelated dirty
worktree failure is external: preserve it and use an isolating check if possible.

## Worktree, commits and proportionate validation

Before every lot inspect `git status --short`, branch, relevant diff and the prior
handoff. Stage explicit paths only; never `git add -A`, a broad formatter, a lockfile
refresh for tidiness, or someone else's change. Default to one coherent commit per
completed lot (`feat(behave): ...`, `fix(behave): ...`, or `docs(behave): ...`). Audit
`git diff --cached --name-only` and `git diff --cached --check` before each commit.
Never reset, rebase, force-push, amend another author, push, or publish externally
without explicit authorization.

Each lot runs its smallest causal checks: contract/parser checks for an input contract;
lowering, worker manifest and output-validator checks for an engine path; WAL/replay and
evaluation-capture checks for a persisted-proof path; link/claim checks for docs. Record
commands actually run and what each proves. Do not run the global repository gate after
each lot. Lot 10 alone runs the required integrated checks, including `deno task
check`
and `deno task check:ui` only if UI sources changed, plus all affected focused and
persisted replay checks.

## Definition of done

The core demo is complete only when one exact lamp project has an inspectable bounded
question, server-owned architecture/parameter authority, exact geometry where needed,
distinct mechanical/thermal/electrical evidence at their true authority levels, one
reviewed cross-domain change with explicit impact, a read-only Workbench projection of
persisted facts, and human decisions only where actually recorded. A static result
cannot prove impact; a documentary Modelica run cannot be called a thermal requirement
verdict; ngspice cannot become an oracle; a UI card cannot create proof.

Each vertical independently closes review → sealed inputs → qualified execution →
captured observations → applicable evaluation → WAL/replay. Otherwise the demo remains
blocked at its precise literal state. The final branch reconciles living
coverage/how-to/reference pages with implemented behavior and explicit exclusions.

## Progress journal

Update this small ledger in the same atomic commit as each lot. It is a handoff index,
not a substitute for persisted evidence.

| Lot | Status                | Commit                                                                                                 | Validation and proof pointer                                                                                                                                                                        | Next / human gate                          |
| --- | --------------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 01  | observed complete     | P01 `0488d98`; P02 `d2dcdac`; P03 `7d1c268`; live walk through r227/r26 2026-08-23                      | AL01 brief r4 / project r227 / Thread r26; three sibling questions remain separate; [runtime evidence](../../projects/articulated-led-desk-lamp/runtime-evidence.md) | living tracking pages                      |
| 02  | observed complete     | A01–A02 `98b57bf`; A03 probe; live A10 plus later arm/LampHead requirements                             | structure rendered/reread at Thread r3, PartDefinitions r4, arm requirements r5, LampHead requirements r14; typed/value probe stays `unresolved` | A04 typed/value remains `unresolved`       |
| 03  | observed complete (pass path) | M01 then live G2 → STEP r7 → proof r9 → CalculiX `@3` r10 → L5 r11                             | canonical STEP `493af847…`; displacement `0.27238935341620824 mm` and stress `6.876467452777839 MPa`; L4 `pass` vs `2 mm` / `80 MPa`; human L5 r11. G3 fail-only correction did not fire | no correction loop; not a whole-lamp verdict |
| 04  | observed complete     | T05–T17 code plus live G4 → L3 r13 → L4 r16 → L5 r17                                                   | admitted OMC 1.27.0/DASSL `305.1378579691034 K`; L4 `pass` margin `7.862142030896621 K` vs `313 K`; human L5 r17. Isolated `5 W`; not a spatial thermal proof | G6 does not mutate this admission          |
| 05  | observed complete (admitted SPICE) | E01 `99b23f8`; D2 preflight `1e4adf8`; live G5 → L3 r19 → L4 r21 → L5 r22                   | `simulate.run-admitted-spice@1` ngspice 42 `i(v1)=-0.028827 A`; L4 derives `0.028827 A` and `0.345924 W`; all named G5 criteria `pass`; human L5 r22. `mcp-spice` stays `unresolved` preflight | not mcp-spice; not safety/EMC              |
| 06  | observed complete except X10 | X01–X11 code; live G6 r23–r26 2026-08-23                                                            | r23 seal; r24 proposes electrical `invalidated`, thermal `invalidated`, mechanical `carried-forward`; YOLO X09 r25 applies those exact statuses; X11 r26 `carried-forward`; `rerunProposals: none`. G6 is a proposed future coupling input, not a thermal result | X10 `unavailable`; no hidden rerun         |
| 07  | observed GET/SSE complete | U07 `f60a53e`; L5 card `c746590`; fix `90d0773`; live read 2026-08-23                                | GET `engineering-workbench/0.3` evidence, `X-Casys-Data-Source: canonical-thread-snapshot`, `source observed`, `aligned` 26/26; SSE `workbench-snapshot` id `articulated-led-desk-lamp-al01:227:26:13`. Dedicated U01–U06/U08–U09 presentation lots remain RFC-internal | Workbench stays read-only                  |
| 08  | in progress (R07)     | R06 `402bc22`; R07 `a2a802d`                                                                            | isolated FEA remaining identity projections and completed-replay verification are a pure collaborator; CAS, WAL, CalculiX/SysON I/O remain adapter-owned; adapter 1137→942; focused tests: 15 pass | R08 or explicit park                       |
| 09  | complete              | H03 `fc55d9f`; H04 grid `a2c35b8`; H04 names `a946e5e`; H04 stack safety `ebb2b88`; H05 audit 2026-08-22; H06 `d6a32b1`; H07 `ba4da10`; H08 catalog root confinement in this commit | H08 fail-closed `realPath` descendant check: `linked/case.json` directory-symlink escape rejected; absent/malformed/id-mismatch unchanged; focused reader tests: 4 pass | —                                          |
| 10  | observed complete     | AL01 live walk through project r227 / Thread r26 plus this commit                                      | `deno task --frozen check` passed; `git diff --check` passed; Cursor Grok 4.6 xhigh Fast final read-only review: `MERGE`; GET/SSE Workbench aligned 26/26 | no human gate; X10 remains `unavailable`   |
| 11  | in progress           | current worktree                                                                                        | Breaking activity/revision/attempt refactor authorized 2026-08-23; implementation delegated to Grok 4.6 `xhigh`                                                               | fresh post-refactor validation project     |

## Required handoff

After every lot, leave this exact compact record in the response and update its journal
row:

```text
Lot: NN — <name>
Status: complete | parked | blocked
Commit: <sha> | not committed (reason)
Changed paths: <explicit list>
Authority delta: <registered contract/operation, or none>
Persisted proof delta: <artifact/WAL/replay identity, or none>
Validation: <command> — pass|fail; <what it establishes>
Known literal gaps: <none or exact state and identity>
Next: <first permitted queue item>
Human input required: <none or one exact decision>
```

A successor agent must be able to tell from this record whether automatic continuation
is allowed, without trusting private terminal history.
