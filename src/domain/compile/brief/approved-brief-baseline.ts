/**
 * Reviewed identity of `baseline.from-approved-brief@1`.
 *
 * Application dispatch and orchestration materialization share this exact
 * operation identity. Capture bytes and executors stay outside the domain.
 */
export const APPROVED_BRIEF_BASELINE_OPERATION = {
  id: "baseline.from-approved-brief",
  version: "1",
} as const;
