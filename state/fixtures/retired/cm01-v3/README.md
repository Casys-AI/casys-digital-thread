# CM-01 V3 retired golden fixture

This directory preserves the exact reviewed JSON bytes for the closed CM-01 V3
demonstration. It is historical evidence only: no runtime code, operation, gate, task,
or project admission reads this fixture.

Verify integrity from the repository root:

```sh
shasum -a 256 -c state/fixtures/retired/cm01-v3/SHA256SUMS
```

The fixture must not be edited to create or qualify a new project. A future generic
product path needs its own reviewed evidence and admission.
